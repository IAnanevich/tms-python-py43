# Напишите пользовательскую функцию pow3_func(x), которая должна принимать в качестве аргумента число x и
# возводить его в куб. В случае успешных вычислений функция должна выводить на экран сообщение в формате «x^3 = res»
# и возвращать res. В противном случае должно выводиться соответствующее сообщение об ошибке и возвращаться None.
# Проверьте функцию, вызвав ее с аргументами 5 и '5'. Для решения задачи используйте в теле
# функции инструкцию try/except/else.
import result2


def pow3_func(x):
    try:
        x = float(x)  # Преобразуем введенное значение в число (возможно, как целое, так и с плавающей точкой)
        result = x ** 3
        print(f"{x}^3 = {result}")
        return result
    except (ValueError, TypeError):
        print("Ошибка: Пожалуйста, введите числовое значение.")
        return None


# Примеры использования функции:
result1 = pow3_func(5)
result2 = pow3_func('5' if isinstance(result2, float) else None)
